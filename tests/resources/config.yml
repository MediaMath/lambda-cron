all:
  bucket: 'test-bucket-all-{environment}'
  alarm:
    enabled: False
  every:
    hours: 2

prod:
  bucket: 'test-bucket-custom'
  alarm:
    enabled: True
    email: my@email.com
  every:
    minutes: 5

staging:
  not_used_key: not_used_value

alarm_bad_value:
  alarm:
    enabled: bad

frequency_error:
  every:
    minutes: 5
    hours: 1
